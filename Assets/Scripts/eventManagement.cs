using UnityEngine;
using System.Collections;

public class eventManagement : MonoBehaviour {
	public GameObject boxPrefab;
	public GameObject theCanvas;
	// Use this for initialization
	void Start () {
		//GameObject clone;
		//clone = Instantiate(boxPrefab, transform.position, transform.rotation) as GameObject;
		//clone.transform.parent = theCanvas.transform;
	}
	
	// Update is called once per frame
	void Update () {
	
	}

	public void nextBox () {
		//GameObject clone;
		//clone = Instantiate(boxPrefab, transform.position, transform.rotation) as GameObject;
		//clone.transform.parent = theCanvas.transform;
	}


}	
